name: Slimmer LaTeX compiler

on:
  #triggered when PR is (re)opened or synchronized (new commit to head)
  pull_request:
    types: [opened, reopened, synchronize]
    paths:
      - '**.tex'

jobs:
  compile-latex:
    runs-on: ubuntu-latest

    steps:
      - name: Set up Git repository
        uses: actions/checkout@v3
      - name: List changed files
        id: files
        uses: jitterbit/get-changed-files@v1
        with:
          format: 'space-delimited'
      - name: Extract path
        id: path
        run: |
          FILES="${{ steps.files.outputs.added_modified }}"
          echo "FILE_PATH=$(dirname "$files")" >> $GITHUB_OUTPUT
      - name: Extract name
        id: name
        run: |
          FILES="${{ steps.files.outputs.added_modified }}"
          echo "FILE_PATH=${FILES&&.*}" >> $GITHUB_OUTPUT
      - name: Compile LaTeX document
        uses: xu-cheng/latex-action@v3
        with:
          root_file: |
            ${{ steps.files.outputs.added_modified }}
          working_directory: ${{ steps.version.outputs.FILE_PATH }}
      - name: Upload PDF file
        uses: actions/upload-artifact@v3
        with:
          name: PDF
          path: ${{ steps.path.outputs.FILE_PATH }}/${{ steps.name.outputs.FILE_NAME }}.pdf
